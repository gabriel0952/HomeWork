<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".MainActivity">

    <ImageView
        android:id="@+id/imageView"
        android:layout_width="200dp"
        android:layout_height="200dp"
        android:layout_marginBottom="60dp"
        app:layout_constraintBottom_toTopOf="@+id/user_name_textView"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:srcCompat="@drawable/medical_mask_icon" />

    <EditText
        android:id="@+id/user_password_editText"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:ems="10"
        android:hint="Password"
        android:inputType="textPersonName"
        android:password="true"
        app:layout_constraintBottom_toTopOf="@+id/login_button"
        app:layout_constraintStart_toStartOf="@+id/login_button" />

    <TextView
        android:id="@+id/password_textView"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Password"
        android:textColor="#2196F3"
        app:layout_constraintBottom_toTopOf="@+id/user_password_editText"
        app:layout_constraintStart_toStartOf="@+id/user_password_editText" />

    <Button
        android:id="@+id/login_button"
        android:layout_width="213dp"
        android:layout_height="49dp"
        android:layout_marginBottom="100dp"
        android:backgroundTint="#03A9F4"
        android:text="LOGIN"
        android:textColor="#FFFFFF"
        android:textSize="20sp"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent" />

    <TextView
        android:id="@+id/user_name_textView"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Account"
        android:textColor="#2196F3"
        app:layout_constraintBottom_toTopOf="@+id/user_account_editText"
        app:layout_constraintStart_toStartOf="@+id/user_account_editText" />

    <EditText
        android:id="@+id/user_account_editText"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:ems="10"
        android:hint="Account"
        android:inputType="textPersonName"
        app:layout_constraintBottom_toTopOf="@+id/password_textView"
        app:layout_constraintStart_toStartOf="@+id/password_textView" />

</androidx.constraintlayout.widget.ConstraintLayout>